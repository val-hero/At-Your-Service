<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.AtYourService" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.AtYourService" parent="Base.Theme.AtYourService" />

    <style name="Theme.AtYourServiceSplash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/gray</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_at_your_service_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.AtYourService</item>
    </style>
</resources>